layout_conda() {
    echo "ATTEMPTING TO AUTOLAYOUT CONDA 🔥🔥🔥🔥🔥🔥🔥 WHAT A PILE OF CRAP"

    echo "JUST RUN $ conda activate ${1}"
    # eval "$(conda shell.zsh activate)"

#     source activate base

#   if [ -n "$1" ]; then
#     echo "Given ENV ${1}"
#     # Explicit environment name from layout command.
#     local env_name="$1"
#     source activate ${env_name}
#   elif (grep -q name: environment.yml); then
#     # Detect environment name from `environment.yml` file in `.envrc` directory
#     source activate `grep name: environment.yml | sed -e 's/name: //'`
#   else
#     (>&2 echo No environment specified);
#     exit 1;
#   fi;
}

# # https://github.com/direnv/direnv/issues/98
# # https://tooky.co.uk/using-direnv-and-chruby-together/
# use_ruby() {
#   local ver=$1
#   if [[ -z $ver ]] && [[ -f .ruby-version ]]; then
#     ver=$(cat .ruby-version)
#   fi
#   if [[ -z $ver ]]; then
#     echo Unknown ruby version
#     exit 1
#   fi
#   chruby $ver
# }
